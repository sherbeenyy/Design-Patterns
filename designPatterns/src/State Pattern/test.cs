// var mediaPlayer = new MediaPlayer();

// mediaPlayer.PressPlay();
// Console.WriteLine("Next state: " + mediaPlayer.GetState().GetType().Name);
// mediaPlayer.PressPlay();
// Console.WriteLine("Next state: " + mediaPlayer.GetState().GetType().Name);
// mediaPlayer.PressPlay();
// Console.WriteLine("Next state: " + mediaPlayer.GetState().GetType().Name);
